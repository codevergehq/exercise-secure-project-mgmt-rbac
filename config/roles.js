const PERMISSIONS = {
    // Project Operations
    // Define permissions for creating, reading, updating, and deleting projects

    // Task Operations
    // Define permissions for managing tasks

    // Team Operations
    // Define permissions for team management
};

const ROLES = {
    // Define roles (ADMIN, PROJECT_MANAGER, TEAM_LEAD, DEVELOPER)
    // Specify permissions for each role
};

module.exports = { PERMISSIONS, ROLES };
